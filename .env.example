# Claude API
ANTHROPIC_API_KEY=sk-ant-xxxxx

# Tally.so webhook signing secret
TALLY_SIGNING_SECRET=your-tally-signing-secret

# Results storage directory (default: ./data/results)
DATA_DIR=./data/results

# Omio search (enabled by default)
OMIO_ENABLED=true

# Passenger profiles directory
PASSENGERS_DIR=./data/passengers
